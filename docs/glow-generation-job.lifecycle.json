{
  "schema_version": 1,
  "diagram_type": "lifecycle",
  "meta": {
    "title": "Glow — Durable generation job lifecycle",
    "quality_profile": "showcase",
    "viewBox": [1080, 640],
    "animation": "trace"
  },
  "lanes": [
    { "id": "main", "label": "Generation rail" },
    { "id": "terminal", "label": "Terminal outcome" }
  ],
  "states": [
    { "id": "queued", "type": "start", "label": "Queued", "sublabel": "job row created", "lane": "main", "col": 0, "step": "01", "tag": "pending" },
    { "id": "processing", "type": "active", "label": "Processing", "sublabel": "OpenAI image request", "lane": "main", "col": 2, "step": "02", "tag": "running" },
    { "id": "completed", "type": "success", "label": "Completed", "sublabel": "private result persisted", "lane": "main", "col": 4, "step": "03", "tag": "success" },
    { "id": "failed", "type": "failure", "label": "Failed", "sublabel": "error state persisted", "lane": "terminal", "col": 0, "step": "04", "tag": "terminal" }
  ],
  "transitions": [
    { "id": "dispatch", "from": "queued", "to": "processing", "label": "claim for provider processing", "variant": "emphasis" },
    { "id": "success", "from": "processing", "to": "completed", "label": "persist completed state + private result", "variant": "emphasis", "labelDy": 80 },
    { "id": "error", "from": "processing", "to": "failed", "label": "persist failure + settlement marker", "variant": "security", "labelDy": 24 }
  ],
  "cards": [
    {
      "dot": "cyan",
      "title": "Durable rail",
      "items": [
        "Every job status is persisted in generation_jobs",
        "Provider tracking ids stay attached to the user-owned job row",
        "Polling reads only the current user’s job"
      ]
    },
    {
      "dot": "rose",
      "title": "Failure boundary",
      "items": [
        "Failed is an explicit terminal state",
        "The credits_refunded marker records settlement state for idempotent recovery",
        "Authoritative credit settlement, rate limits, and billing reconciliation remain later gates"
      ]
    }
  ]
}
